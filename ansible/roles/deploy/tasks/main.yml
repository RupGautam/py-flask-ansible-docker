---
- name: Fetch the repo from GitHub
  git:
    repo: 'https://github.com/RupGautam/py-flask-ansible-docker.git'
    clone: yes
    dest: ./flask-app

- name: Docker-compose up -d 
  shell: cd ./flask-app && docker-compose up -d --build --remove-orphans
    
    